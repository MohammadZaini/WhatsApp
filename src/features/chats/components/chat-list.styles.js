import styled from "styled-components";
import { colors } from "../../../infrastructure/theme/colors";

export const NewGroupContainer = styled.View``;

export const NewGroupText = styled.Text`
    color: ${colors.blue}
    font-size: 17px;
    margin-bottom: 5px;
`;